# Optional executable overrides. Normally the adapters resolve `codex` and the
# Claude Agent SDK runtime from the current environment.
PI_AGENT_BRIDGE_CODEX_COMMAND=codex
# PI_AGENT_BRIDGE_CLAUDE_PATH=/absolute/path/to/claude
